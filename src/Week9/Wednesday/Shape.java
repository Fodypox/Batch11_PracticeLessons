package Week9.Wednesday;

public abstract class Shape {
    public abstract double calculateArea();
/**
 * Create a shape hierarchy with an abstract Shape class and multiple subclasses
 * (e.g., Circle, Rectangle, Triangle). Each subclass should override a method such as calculateArea()
 * to compute its area.*/
}
